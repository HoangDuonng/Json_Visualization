import { LOCAL_FONT_PROTOCOL } from "@jsondraw/common";

import { type JsonDrawFontFaceDescriptor } from "../Fonts";

export const EmojiFontFaces: JsonDrawFontFaceDescriptor[] = [
  {
    uri: LOCAL_FONT_PROTOCOL,
  },
];
